import React from 'react';
import './style.css';

export default function Home() {
    return (
        <div>
            <h1>Home</h1>
            <p>
                We should be able to set up a router, in order to load different
                components in the same root div basing on the url, without refeshing the
                whole application
            </p>
        </div>
    );
}
